# دليل المساهمة في Nebras Platform

نشكرك على اهتمامك بالمساهمة في **Nebras Platform**! نحن نرحب بجميع أنواع المساهمات، سواء كانت إصلاح أخطاء، إضافة ميزات جديدة، تحسين الوثائق، أو حتى الإبلاغ عن المشاكل.

## جدول المحتويات

- [قواعد السلوك](#قواعد-السلوك)
- [كيف يمكنني المساهمة](#كيف-يمكنني-المساهمة)
- [الإبلاغ عن المشاكل](#الإبلاغ-عن-المشاكل)
- [اقتراح ميزات جديدة](#اقتراح-ميزات-جديدة)
- [عملية المساهمة بالكود](#عملية-المساهمة-بالكود)
- [معايير الكود](#معايير-الكود)
- [عملية المراجعة](#عملية-المراجعة)

---

## قواعد السلوك

هذا المشروع يلتزم بقواعد سلوك محددة لضمان بيئة ترحيبية ومحترمة للجميع. من خلال المساهمة، فإنك توافق على الالتزام بهذه القواعد. يرجى قراءة [قواعد السلوك](CODE_OF_CONDUCT.md) قبل المساهمة.

باختصار، نتوقع من جميع المساهمين:
- **الاحترام المتبادل**: التعامل مع الآخرين بلطف واحترام
- **التواصل البنّاء**: تقديم ملاحظات بناءة ومفيدة
- **الانفتاح**: الترحيب بوجهات النظر المختلفة
- **التركيز على المشروع**: الحفاظ على المناقشات متعلقة بالمشروع

---

## كيف يمكنني المساهمة

هناك عدة طرق للمساهمة في المشروع، وكل مساهمة مهمة بغض النظر عن حجمها:

### 1. الإبلاغ عن المشاكل (Bug Reports)

إذا وجدت مشكلة في المشروع، يمكنك المساعدة بالإبلاغ عنها. المشاكل المُبلغ عنها بشكل جيد تساعد في تحسين المشروع بشكل كبير.

### 2. اقتراح ميزات جديدة (Feature Requests)

إذا كانت لديك فكرة لميزة جديدة تعتقد أنها ستحسن المشروع، نحن نرحب باقتراحاتك.

### 3. تحسين الوثائق (Documentation)

الوثائق الجيدة مهمة جداً. يمكنك المساهمة بتحسين الوثائق الحالية، إضافة أمثلة، أو توضيح نقاط غامضة.

### 4. المساهمة بالكود (Code Contributions)

إذا كنت مطوراً، يمكنك المساهمة بإصلاح الأخطاء، إضافة ميزات جديدة، أو تحسين الأداء.

### 5. المراجعة (Code Review)

مراجعة Pull Requests المفتوحة ومساعدة المساهمين الآخرين هي طريقة رائعة للمساهمة.

---

## الإبلاغ عن المشاكل

عند الإبلاغ عن مشكلة، يرجى اتباع الإرشادات التالية لمساعدتنا في فهم المشكلة وحلها بسرعة:

### قبل الإبلاغ

1. **تحقق من Issues الموجودة**: تأكد من أن المشكلة لم يتم الإبلاغ عنها مسبقاً
2. **تحقق من الوثائق**: تأكد من أنك تستخدم المشروع بشكل صحيح
3. **حدّث المشروع**: تأكد من أنك تستخدم أحدث إصدار

### عند الإبلاغ

قم بإنشاء Issue جديد وقدم المعلومات التالية:

**العنوان**: عنوان واضح ومختصر يصف المشكلة

**الوصف**: يجب أن يتضمن:
- **وصف المشكلة**: ما هي المشكلة بالضبط؟
- **خطوات إعادة الإنتاج**: كيف يمكن إعادة إنتاج المشكلة؟
  1. الخطوة الأولى
  2. الخطوة الثانية
  3. النتيجة المتوقعة مقابل النتيجة الفعلية
- **البيئة**: نظام التشغيل، المتصفح، إصدار Node.js، إلخ
- **لقطات الشاشة**: إذا كانت مفيدة
- **رسائل الخطأ**: نسخ كامل من رسائل الخطأ

**مثال**:
```
العنوان: خطأ في تحميل الصفحة الرئيسية على Safari

الوصف:
عند فتح index.html على متصفح Safari، تظهر رسالة خطأ ولا يتم تحميل المحتوى.

خطوات إعادة الإنتاج:
1. فتح Safari على macOS
2. فتح ملف index.html
3. ملاحظة رسالة الخطأ في Console

البيئة:
- نظام التشغيل: macOS Ventura 13.2
- المتصفح: Safari 16.3
- إصدار المشروع: v1.0.0

رسالة الخطأ:
TypeError: Cannot read property 'style' of null
```

---

## اقتراح ميزات جديدة

نرحب باقتراحات الميزات الجديدة! عند اقتراح ميزة:

### قبل الاقتراح

1. **تحقق من Issues الموجودة**: قد يكون شخص آخر قد اقترح نفس الميزة
2. **تأكد من ملاءمة الميزة**: هل الميزة تتناسب مع أهداف المشروع؟

### عند الاقتراح

قم بإنشاء Issue جديد مع التسمية "enhancement" وقدم:

- **وصف الميزة**: ما هي الميزة المقترحة؟
- **المشكلة التي تحلها**: لماذا هذه الميزة مفيدة؟
- **الحل المقترح**: كيف يمكن تنفيذ هذه الميزة؟
- **البدائل**: هل هناك طرق بديلة لتحقيق نفس الهدف؟
- **أمثلة**: أمثلة من مشاريع أخرى إن وجدت

---

## عملية المساهمة بالكود

إذا كنت ترغب في المساهمة بالكود، اتبع الخطوات التالية:

### 1. إعداد البيئة التطويرية

```bash
# Fork المستودع على GitHub أولاً

# استنساخ Fork الخاص بك
git clone https://github.com/YOUR_USERNAME/nebras-platform.git
cd nebras-platform

# إضافة المستودع الأصلي كـ upstream
git remote add upstream https://github.com/AbdulElahOthmanGwaith/nebras-platform.git

# تثبيت الحزم المطلوبة
npm install
```

### 2. إنشاء فرع جديد

قم دائماً بإنشاء فرع جديد لكل ميزة أو إصلاح:

```bash
# تحديث الفرع الرئيسي
git checkout main
git pull upstream main

# إنشاء فرع جديد
git checkout -b feature/your-feature-name
# أو
git checkout -b fix/your-bug-fix
```

**تسمية الفروع**:
- `feature/` للميزات الجديدة (مثل: `feature/add-dark-mode`)
- `fix/` لإصلاح الأخطاء (مثل: `fix/navigation-bug`)
- `docs/` لتحديثات الوثائق (مثل: `docs/update-readme`)
- `refactor/` لإعادة هيكلة الكود (مثل: `refactor/optimize-styles`)

### 3. إجراء التغييرات

- اكتب كود نظيف وواضح
- اتبع معايير الكود المحددة أدناه
- أضف تعليقات توضيحية عند الحاجة
- اختبر التغييرات بشكل شامل

### 4. Commit التغييرات

اكتب رسائل commit واضحة ومفيدة:

```bash
git add .
git commit -m "نوع: وصف مختصر للتغيير

وصف تفصيلي اختياري للتغيير.
يمكن أن يتضمن عدة أسطر.

Fixes #123"
```

**أنواع Commits**:
- `feat:` ميزة جديدة
- `fix:` إصلاح خطأ
- `docs:` تحديث وثائق
- `style:` تغييرات في التنسيق (لا تؤثر على الكود)
- `refactor:` إعادة هيكلة الكود
- `test:` إضافة أو تحديث اختبارات
- `chore:` مهام صيانة

**مثال**:
```bash
git commit -m "feat: إضافة وضع الظلام للواجهة

- إضافة زر للتبديل بين الوضع الفاتح والداكن
- حفظ تفضيلات المستخدم في localStorage
- تحديث الأنماط لدعم كلا الوضعين

Fixes #45"
```

### 5. Push التغييرات

```bash
git push origin feature/your-feature-name
```

### 6. فتح Pull Request

1. اذهب إلى Fork الخاص بك على GitHub
2. اضغط على "Compare & pull request"
3. املأ نموذج Pull Request بالمعلومات المطلوبة:
   - **العنوان**: عنوان واضح ومختصر
   - **الوصف**: شرح تفصيلي للتغييرات
   - **النوع**: ميزة جديدة، إصلاح خطأ، إلخ
   - **الاختبارات**: كيف تم اختبار التغييرات
   - **Screenshots**: إذا كانت التغييرات بصرية
   - **Issues المرتبطة**: رقم Issue إن وجد

---

## معايير الكود

للحفاظ على جودة الكود واتساقه، يرجى اتباع المعايير التالية:

### HTML

- استخدم HTML5 semantic tags
- استخدم indentation بـ 4 مسافات
- أضف alt text لجميع الصور
- تأكد من accessibility

```html
<!-- جيد -->
<section class="hero">
    <h1>عنوان رئيسي</h1>
    <p>نص توضيحي</p>
</section>

<!-- سيء -->
<div class="hero">
<h1>عنوان رئيسي</h1>
<p>نص توضيحي</p>
</div>
```

### CSS

- استخدم CSS variables للألوان والقيم المتكررة
- اتبع نهج mobile-first
- استخدم أسماء classes واضحة ووصفية
- تجنب !important إلا عند الضرورة القصوى

```css
/* جيد */
:root {
    --primary-color: #38BDF8;
    --text-color: #F8FAFC;
}

.hero-section {
    background-color: var(--primary-color);
    color: var(--text-color);
}

/* سيء */
.hs {
    background-color: #38BDF8 !important;
}
```

### JavaScript

- استخدم ES6+ features
- استخدم const و let بدلاً من var
- اكتب أسماء متغيرات ودوال واضحة
- أضف تعليقات للكود المعقد
- تجنب global variables

```javascript
// جيد
const calculateTotal = (items) => {
    return items.reduce((sum, item) => sum + item.price, 0);
};

// سيء
function calc(x) {
    var t = 0;
    for (var i = 0; i < x.length; i++) {
        t = t + x[i].p;
    }
    return t;
}
```

### الاختبارات

- اكتب اختبارات لجميع الميزات الجديدة
- تأكد من نجاح جميع الاختبارات قبل Push
- استخدم أسماء اختبارات وصفية

```javascript
// جيد
test('يجب أن يحسب المجموع الكلي بشكل صحيح', async () => {
    const items = [{ price: 10 }, { price: 20 }];
    const total = calculateTotal(items);
    expect(total).toBe(30);
});
```

---

## عملية المراجعة

بعد فتح Pull Request:

1. **المراجعة التلقائية**: سيتم تشغيل GitHub Actions تلقائياً للتحقق من:
   - نجاح الاختبارات
   - جودة الكود
   - عدم وجود conflicts

2. **المراجعة البشرية**: سيقوم أحد المشرفين بمراجعة الكود وقد يطلب تعديلات

3. **التعديلات**: إذا طُلبت تعديلات:
   - قم بإجراء التعديلات المطلوبة
   - Commit و Push التغييرات الجديدة
   - سيتم تحديث Pull Request تلقائياً

4. **الدمج**: بعد الموافقة، سيتم دمج Pull Request في الفرع الرئيسي

### وقت المراجعة

نحاول مراجعة Pull Requests في أسرع وقت ممكن، لكن قد يستغرق الأمر بضعة أيام. يرجى التحلي بالصبر.

---

## نصائح إضافية

- **ابدأ صغيراً**: إذا كنت مساهماً جديداً، ابدأ بمهام صغيرة مثل إصلاح أخطاء إملائية أو تحسين وثائق
- **اسأل قبل البدء**: إذا كنت تخطط لعمل كبير، افتح Issue أولاً لمناقشة الفكرة
- **كن صبوراً**: المراجعة قد تستغرق وقتاً
- **تعلم من الملاحظات**: الملاحظات على الكود فرصة للتعلم والتحسين

---

## الحصول على المساعدة

إذا كنت بحاجة إلى مساعدة:

- **افتح Issue**: للأسئلة العامة أو المشاكل
- **راجع الوثائق**: قد تجد الإجابة في README أو الوثائق الأخرى
- **تواصل مع المشرفين**: عبر GitHub

---

## الشكر

شكراً لك على مساهمتك في **Nebras Platform**! كل مساهمة، مهما كانت صغيرة، تساعد في تحسين المشروع وتجعله أفضل للجميع.

نحن نقدر وقتك وجهدك! 🙏
